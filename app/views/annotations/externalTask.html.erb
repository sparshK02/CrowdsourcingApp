<% provide(:title, "Complete Task") %>
<% id = params[:annotation_id] || params[:id] %> 
<%@annotation = Annotation.find(id)%>

<div class = "full-page">

<div class="sticky-top">
<h1>Instructions: </h1>
<p> Attempt to find some external sources that have the answer to this question, if there are 
not any external sources, leave this section blank. </p>
<p>If you are able to find external sources with an answer to the original question, 
please enter them in the box with a format like: "Source: URL", in the case of reddit: "Reddit: https://www.reddit.com/". </p>
<p> The Product, Item ID, and original Question are given to you below: </p>
<h3> <b>Product:</b> <%= @annotation.product%><h3>
<h3> <b>Item ID:</b> <%= @annotation.itemID%><h3>
<h3> <b>Question:</b> <%= @annotation.question%><h3>
</div>

<div class="page-content">

<%= form_with(model: @annotation)do |f| %>
<br>
<br>

<h3><%= f.label "Enter any external sources you found here: "%></h3>
<%= f.text_field :externalSources, placeholder: "Ex: \"Reddit: https://www.reddit.com/\""%>


<br>

<%= f.hidden_field :formStage, value: 5%>
<%= f.hidden_field :finished, value: true%> 
<%= f.hidden_field :annotatorID, value: current_user.annotatorID%>


<div class="actions">
    <%= link_to "Go Back", complete_task4_path(@annotation), class: 'go-back'%>
    <%= f.submit "Submit", class: 'next-section'%>
  </div>

<% end %>

</div>
</div>


